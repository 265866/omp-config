providers:
  cliproxy:
    baseUrl: https://cliproxyapi.netbird.colton.run/v1
    api: openai-completions
    apiKey: "!printenv CLIPROXY_API_KEY"
    authHeader: true
    discovery:
      type: openai-models-list
